div(ng-show='isFinished')
    md-button.md-warn.md-raised(ng-click='action()', focus-on='isFinished')
        | Next person
        span(ng-show='timeout > 0') ({{ timeout }})
        span.glyphicon.glyphicon-chevron-right